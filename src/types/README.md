# Types

- **`database.types.ts`** (généré en J1) — types TypeScript dérivés du schéma Supabase
  (via `generate_typescript_types`). Ne pas éditer à la main : régénérer après chaque
  migration.
- Types applicatifs additionnels (enrichissements, DTOs) à ajouter ici au besoin.
